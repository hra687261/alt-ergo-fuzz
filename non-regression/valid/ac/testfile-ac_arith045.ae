(*(set-info :status valid)*)


logic a,b,c,d,e,gamma,beta,x,y,t:int
logic ac f,g : int,int -> int

goal g45 :
  f(a+1,b) = gamma ->
  f(a+t,c) = beta ->
  (* f(gamma,c) = f(beta,b) est d�duit *)
  g(f(gamma,c),d) = x ->
  g(f(beta,b),e) = y ->
  t = 1 ->
  g(x,e) = g(y,d)

