(*(set-info :status valid)*)


logic a,b,c,d,e,gamma,beta,x,y:int
logic ac f,g : int,int -> int

goal g73 :
  f(a,b) = gamma ->
  f(a,c) = beta ->
  (* f(gamma,c) = f(beta,b) est d�duite *)
  f(f(gamma,c),d) = x ->
  f(f(beta,b),e) = y ->
  f(x,e) = f(y,d)

